/* 
Создай переменную myVariable и условие if else, по которому консолит: "Значение переменной не было присвоено" или "Значение переменной было присвоено". */

let myVariable;
if (myVariable === undefined) {
  console.log("Значение переменной не было присвоено");
} else {
  console.log("Значение переменной было присвоено");
}